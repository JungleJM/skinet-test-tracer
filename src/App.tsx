import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="baseline-panel" aria-labelledby="baseline-title">
        <p className="eyebrow">SkiNet practice app</p>
        <h1 id="baseline-title">Baseline is ready</h1>
        <p>
          This app is intentionally minimal. Feature work starts after this
          scaffold is committed.
        </p>
      </section>
    </main>
  )
}

export default App
